{
  "name": "btc-insurance-contract",
  "version": "1.0.0",
  "description": "Bitcoin-native protection vault smart contract for OPNet",
  "type": "module",
  "scripts": {
    "build": "asc assembly/index.ts --target release --measure --uncheckedBehavior never",
    "build:debug": "asc assembly/index.ts --target debug --measure"
  },
  "dependencies": {
    "@btc-vision/as-bignum": "^0.1.2",
    "@btc-vision/btc-runtime": "^1.11.0-rc.10"
  },
  "devDependencies": {
    "@btc-vision/opnet-transform": "latest",
    "assemblyscript": "latest"
  }
}
